<p id="notice"><%= notice %></p>

<h1>야시장-시장 페이지(/market/market)</h1><br>

<h1><%= @market.name %></h1>

---------------------- 이미지 칸 ----------------------
<p>
  이미지
</p>

---------------------- 시장 정보 설명 칸 ----------------------
<p>
  <strong>위치</strong>
  <%= @market.address %><br>

  <strong>영업시간</strong>
  <%= @market.open %><br>

  <strong>주차</strong>
  <% if @market.parking == true %>
    가능
  <% else %>
    불가
  <% end %>
  <br>

  <strong>전화번호</strong>
  <%= @market.tel %><br>

  <strong>휴일</strong>
  <%= @market.closed %><br>

  <%= link_to 'Edit', edit_market_path(@market) %> |
  <%= link_to 'Back', markets_path %>
</p>

---------------------- 가게 목록 칸 ----------------------
<p>
  <table>
    <thead>
      <tr>
        <th>사진</th>
        <th>가게이름(대표메뉴)</th>
        <th>부가설명(자유)</th>
        <th>전화번호</th>
        <th><%= link_to 'New', new_shop_path %></th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @market.shops.each do |shop| %>
        <tr>
          <td>사진</td>
          <td><%= shop.name %></td>
          <td>부가설명(자유)</td>
          <td><%= shop.tel %></td>
          <td><%= link_to 'Show', shop %></td>
          <td><%= link_to 'Edit', edit_shop_path(shop) %></td>
          <td><%= link_to 'Destroy', shop, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</p>

---------------------- 리뷰 칸 ----------------------
<p>
  <table>
    <thead>
      <tr>
        <th>제목</th>
        <th>내용</th>
        <th>별점</th>
        <th>날짜</th>
        <th><%= link_to 'New', new_shop_review_path %></th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @market.market_reviews.each do |market_review| %>
        <tr>
          <td><%= market_review.title %></td>
          <td><%= market_review.content %></td>
          <td><%= market_review.rate %></td>
          <td><%= market_review.date %></td>
          <td><%= link_to 'Show', market_review %></td>
          <td><%= link_to 'Edit', edit_market_review_path(market_review) %></td>
          <td><%= link_to 'Destroy', market_review, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <%= link_to '전체 후기 보러가기', market_reviews_path %>
</p>
